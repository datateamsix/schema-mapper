# Schema Mapper - Environment Variables
#
# Copy this file to .env and fill in your credentials.
# Never commit .env to version control!
#
# These variables are automatically loaded when using ConnectionConfig:
#   config = ConnectionConfig('connections.yaml', env_file='.env')

# ==================== SNOWFLAKE ====================
SNOWFLAKE_USER=your_username
SNOWFLAKE_PASSWORD=your_password
SNOWFLAKE_ACCOUNT=abc123.us-east-1

# ==================== BIGQUERY ====================
GCP_PROJECT_ID=my-gcp-project
BQ_CREDENTIALS_PATH=/path/to/service-account-key.json

# ==================== POSTGRESQL ====================
PG_HOST=localhost
PG_USER=postgres
PG_PASSWORD=your_password

# ==================== REDSHIFT ====================
REDSHIFT_HOST=my-cluster.abc123.us-east-1.redshift.amazonaws.com
REDSHIFT_USER=admin
REDSHIFT_PASSWORD=your_password

# ==================== SQL SERVER ====================
MSSQL_SERVER=myserver.database.windows.net
MSSQL_USER=admin
MSSQL_PASSWORD=your_password

# ==================== NOTES ====================
#
# 1. Security:
#    - Never commit .env to git
#    - Add .env to .gitignore
#    - Use different .env files for dev/staging/prod
#
# 2. Usage:
#    - Variables are automatically interpolated in connections.yaml
#    - Use ${VAR_NAME} in YAML to reference these variables
#    - python-dotenv automatically loads .env when ConnectionConfig initializes
#
# 3. Alternative: System environment variables
#    - You can also set these in your shell:
#      export SNOWFLAKE_USER=myuser
#    - Or use cloud secret managers in production
